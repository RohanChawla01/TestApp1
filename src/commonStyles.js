import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  parentContainer: {
    flex: 1,
    justifyContent: 'center',
  },
  scrollContainer: {
    flex: 1,
  },
  viewContainer: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
  },
});
